export type Status = "active" | "paid" | "completed";
export type PaymentCategory = "expense" | "debt" | "asset_debt" | "investment" | "saving";
export type AssetType = "land" | "house" | "car";
export type Income = { id:string; name:string; amount:number; date:string };
export type Debt = { id:string; name:string; originalPrincipal:number; currentBalance:number; annualInterestRate:number; monthlyPayment:number; paymentDay:number; dueDate?:string; status:Status };
export type Asset = { id:string; name:string; type:AssetType; value:number; financed:boolean; remainingDebt:number; originalDebt?:number; annualInterestRate:number; monthlyPayment:number; paymentDay:number; dueDate?:string; annualMinimumPayment?:number; targetDate?:string; status:Status };
export type Investment = { id:string; name:string; investmentDay:number; monthlyTarget:number; totalInvested:number };
export type InvestmentEntry = { id:string; investmentId:string; month:string; amount:number };
export type Expense = { id:string; name:string; plannedAmount:number; paymentDay:number; status:Status };
export type SavingGoal = { id:string; name:string; targetAmount:number; currentAmount:number; monthlyAmount:number; targetDate?:string; paymentDay:number; status:Status };
export type Insurance = { id:string; company:string; insuredName:string; beneficiary:string; lifeCoverage:number; accidentCoverage:number; criticalIllnessCoverage:number; hospitalDailyCoverage:number; medicalCoverage:number; premiumAmount:number; premiumDay:number; other:{name:string;amount:number}[] };
export type Payment = { id:string; sourceId?:string; title:string; category:PaymentCategory; dueDate:string; plannedAmount:number; actualAmount?:number; paidDate?:string; status:"pending"|"paid"|"overdue"; recurring?:boolean; note?:string };
export type MonthlySnapshot = { month:string; incomes:Income[]; payments:Payment[]; totals:{income:number;allocated:number;actual:number;cashFlow:number;expense:number;debt:number;assetDebt:number;insurance:number;saving:number;investment:number;assets:number;liabilities:number;netWorth:number}; closedAt:string };
export type FinanceState = { incomes:Income[]; debts:Debt[]; assets:Asset[]; investments:Investment[]; investmentEntries:InvestmentEntry[]; expenses:Expense[]; savings:SavingGoal[]; insurance:Insurance[]; payments:Payment[]; closedMonths:MonthlySnapshot[] };
export const emptyState:FinanceState={incomes:[],debts:[],assets:[],investments:[],investmentEntries:[],expenses:[],savings:[],insurance:[],payments:[],closedMonths:[]};
